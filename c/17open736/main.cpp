#include <bits/stdc++.h>
#include <algorithm>

using namespace std;

int main() {
    ifstream cin("cownomics.in");
    ofstream cout("cownomics.out");
    int N, M;
    cin >> N;
    cin >> M;
    vector<string> spottedCow;

    return 0;
}